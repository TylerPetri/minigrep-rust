# MINIGREP
A grep tool used to scan lines of a .txt file, printing the lines including the search word of your choosing\

## Use
`cargo run -- [query] [file]` - to search a word from a file
`cargo test` - to run tests
`cargo run -- [query] [file] > output.txt` - dev tool which creats an output.txt file with all outputs